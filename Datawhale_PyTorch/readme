本项目主要为了储存在Datawhale学习小组学习过程中的学习到的代码，方便以后应用，每个文件开头都存放了k-lab的链接，方便使用时参考。

The main purpose of this project is to store the learned code in the learning process of datawhale learning group, which is convenient for future application. At the beginning of each file, the k-lab link is stored for reference.
